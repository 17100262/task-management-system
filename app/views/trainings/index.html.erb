<p id="notice"><%= notice %></p>
<div class="col-lg-8">
<h1>Trainings</h1>

<table class="table table-bordered white-bg">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trainings.each do |training| %>
      <tr>
        <% if can? :read, training %>
          <td><%= training.name %></td>
          <td><%= link_to 'Show', training, class:"btn btn-primary" %></td>
          <td><%= link_to 'Edit', edit_training_path(training), class:"btn btn-primary" %></td>
          <td><%= link_to 'Delete', training, class:"btn btn-warning", method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Training', new_training_path, class:"btn btn-primary" %>
</div>
</div>