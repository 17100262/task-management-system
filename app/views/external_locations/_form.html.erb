<%= form_with(model: external_location, local: true) do |form| %>
  <% if external_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(external_location.errors.count, "error") %> prohibited this external_location from being saved:</h2>

      <ul>
      <% external_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :external_location_name, class:"form-control" %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address, id: :external_location_address, class:"form-control" %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :external_location_description, class:"form-control" %>
  </div>

  <div class="actions">
    <%= form.submitclass:"login loginmodal-button loginmodal-submit" %>
  </div>
<% end %>
