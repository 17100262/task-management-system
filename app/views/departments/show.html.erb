<p id="notice"><%= notice %></p>
<div class="container">
    <div class="row">
        <div class="loginmodal-container">
            <div class="loginmodal-container-2 pt-3">
              <strong>Name:</strong>
              <%= @department.name %>
            </p>
            
            <center><%= link_to 'Edit', edit_department_path(@department), class:"btn btn-primary" %> 
            <%= link_to 'Back', departments_path, class:"btn btn-primary" %></center>
          </div>
        </div>
    </div>
</div>        
